<h1><%= @missions.name %></h1>


<ul>
<% @missions.each  do |mission| %>

	<li><%= "#{mission.name} #{mission.location} #{mission.description}" %></li>
	<%=link_to"Edit Link", edit_mission_path(mission)%>
	<%=link_to"Go to this Mission", mission_path(mission)%>
	
<% end %>
</ul>