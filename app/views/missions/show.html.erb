<nav class="container-fluid">
    <div class="navbar-header navbar-fixed-top" style="background: rgba(0, 0, 0, 0.8);">
      <a class="navbar-brand" href="#">
       <p>Mission Church</p>
      </a>
      <div class="pull-right">
        <div class="btn-group" role="group">
          <!-- <button class="btn navbar-btn" onclick="location.href='home/sign_up';">Sign In</button> -->
        </div>
        <div class="btn-group" role="group">
          <!-- <button class="btn navbar-btn" onclick="location.href='home/login';">Log In</button> -->
           <%= link_to "Log out", "/volunteers/:id", :method => :delete, :class => "btn navbar-btn" %>
        </div>
      </div>
    </div>


<h1 class="tlt">Mission Name: <%= @missions.name %></h1>
<h2 class="tlt">Mission Location: <%= @missions.location %></h2>
<h2 class="tlt">Mission Description: <%= @missions.description %></h2>


<button onclick="location.href='/missions';">Missions List</button>


<form action="/missions/<%= @missions.id %>/volunteers" method="POST">
<p>Volunteers Join Here</p>
	<button type="submit">Join Mission</button>
<input type="hidden" name="authenticity_token"
      value="<%= form_authenticity_token %>">
</form>

<form action="/missions/<%= @missions.id %>/ministries" method="POST">
<p>Ministries Join Here</p>
	<button type="submit">Join Mission</button>
<input type="hidden" name="authenticity_token"
      value="<%= form_authenticity_token %>">
</form>
<h3>People Already In <%=@missions.name %></h3>

<table class="table">
  <thead class="thead-inverse">
    <tr>
      <th>#</th>
      <th>Name</th>
      <th>Email</th>
      <th>Address</th>
    </tr>
  </thead>
  <tbody>

<% @missions.volunteers.each do | volunteer | %>
	

 <tr>
      <td scope="row"><%=volunteer.id %></th>
      <td><%=volunteer.name %></td>
      <td><%=volunteer.email %></td>
      <td><%=volunteer.address %></td>
<!-- 		<%= volunteer.name %><br> -->
</tr>
<% end %>
</tbody>

		
</table>
	
  <table class="table">
  <thead class="thead-inverse">
    <tr>
      <th>#</th>
      <th>Name</th>
      <th>Email</th>
      <th>Address</th>
    </tr>
  </thead>
  <tbody>

<h3>Ministry Already In <%=@missions.name %></h3>
<% @missions.ministries.each do | ministry | %>
	
 <tr>
      <td scope="row"><%=ministry.id %></th>
      <td><%=ministry.name %></td>
      <td><%=ministry.email %></td>
      <td><%=ministry.address %></td>

</tr>
<% end %>
</tbody>

    
</table>
